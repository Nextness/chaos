type Error flaw
{
    nullPointer{<<You suck and need more RAM, lol!>>};
    outOfMemory;
    outOfBounds;
    somethingElse;
}

func def main()
{
    let AnyFlaw error = Error.nullPointer;
    printfn(<<The error is "{error.string}" - reason: "{error.reason}">>);
    return;
}

module errors;
import <<core::fmt::io.chaos>>;

