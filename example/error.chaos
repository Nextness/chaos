proc main() do
    let error AnyError := make nullPointer! with
        id := «some_id», reason := «reason for the error»
    endmake
    run printfn(«The error is {error.?reason}»)
    return
endproc

#module errors
#import «core::fmt::io.chaos»

