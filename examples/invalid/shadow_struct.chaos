// expect-stage: type
// expect-error: declaration of 'Point' shadows an existing name
// Reusing a visible type name requires '#shadow'.
Point :: struct {
    x: S64;
}

#entry main :: proc -> S64 {
    Point := 5;
    return 0;
}
