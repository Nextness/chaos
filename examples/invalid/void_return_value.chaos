// expect-stage: verify
// expect-error: return with a value in a void procedure
// A value-returning return statement in a void procedure is caught by the
// MIR verifier (the type checker only validates the value against non-void
// result types).
#entry main :: proc {
    return 5;
}
