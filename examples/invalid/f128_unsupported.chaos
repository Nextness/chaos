// expect-stage: target
// expect-error: float type F128 is not yet supported by the fasm backend
// F128 is accepted by the front end but rejected by the fasm backend.
#entry main :: proc -> S64 {
    x: F128 = 1.5;
    return 0;
}
