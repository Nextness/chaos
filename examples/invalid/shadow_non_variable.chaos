// expect-stage: type
// expect-error: declaration of 'Point' shadows an existing name
// Type definitions may be shadowed, but the reuse must be explicit.
Point :: struct {
    x: S64;
}

Point :: struct {
    y: S64;
}
