// expect-exit: 35
// Negative literals: negated constants adapt to typed targets and call args.
#entry main :: proc -> S64 {
    x: S128 = -100;
    y := negate(-7);
    if x == -100 && y == 7 { return 35; }
    return 0;
}

negate :: proc (v: S64) -> S64 {
    return -v;
}
