// expect-exit: 10
// expect-stdout: <empty>
// expect-stderr: <empty>
// A while-style loop: "for true" repeats until a break exits.
#entry main :: proc -> S64 {
    a := 0;
    for true {
        a += 1;
        if a == 10 then break;
    }
    return a;
}
