// expect-exit: 0
// expect-stdout: <empty>
// expect-stderr: <empty>
// Importing core but using only one function: the rest are discarded by
// reachability, so the binary stays small.
#import «core»;
#entry main :: proc -> S64 {
    if !is_digit(48) { return 1; }
    return 0;
}
