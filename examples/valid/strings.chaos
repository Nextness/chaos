// expect-exit: 6
// expect-stdout: <empty>
// expect-stderr: <empty>
// Strings: literal, comparison, and ordering.
#entry main :: proc -> S64 {
    s := «hello»;
    if s == «hello» && s < «world» { return 6; }
    return 0;
}
