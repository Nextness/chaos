#entry main :: proc (argc: S64, argv: []String) {
    some_c_struct: #c Some_Struct;
    some_c_struct.a = 100;

    another_c_struct := #c Some_Struct.{
        100, 100, 200, true
    };

    #c printf(«Hello world, %\n», 10);
    return;
}
