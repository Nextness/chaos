main :: proc (argc: S64, argv: ...String) -> Void {
    error: AnyError = "null-pointer, reason can be included here";
    print(«The error is {error} - reason {error.reason}»);
    return;
}

#module errors;
#import «core::fmt::io.chaos»;

